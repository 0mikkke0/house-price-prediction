# Dataset

The raw Ames Housing dataset is downloaded automatically.

Run:

```bash
python src/download_data.py
```

The downloader uses OpenML dataset ID 42165 and saves the result as `ames_housing.csv`.
